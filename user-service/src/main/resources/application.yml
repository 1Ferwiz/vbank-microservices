spring:
  application:
    name: user-service
  datasource:
    url: jdbc:postgresql://localhost:${DB_PORT:5434}/users_db
    username: ${DB_USER:vbank_admin}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health,info