# Copy this file to ".env" (same folder) and keep .env out of git.
# These are LOCAL DEV credentials only - never used outside your machine.

POSTGRES_USER=vbank_admin
POSTGRES_PASSWORD=changeme_local_dev_only
POSTGRES_PORT=5434
