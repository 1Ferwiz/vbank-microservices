spring:
  application:
    name: account-service
  datasource:
    url: jdbc:postgresql://localhost:${DB_PORT:5434}/accounts_db
    username: ${DB_USER:vbank_admin}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8082

management:
  endpoints:
    web:
      exposure:
        include: health,info

  kafka:
    bootstrap-servers: localhost:${KAFKA_PORT:9094}